<h2>You're now in <%= @user.full_name %>'s personal space</h2>

<dl class="dl-horizontal">
  <dt><strong>名字</strong></dt>
  <dd><%= @user.full_name %></dd>
  <dt><strong>Email</strong></dt>
  <dd><%= @user.email %></dd>
</dl>

<% if @user.stocks.size > 0 %>
 <%= render 'stocks/list' %>
<% else %>
  <p class="lead">The particular individual is uninterested in stocks of any kind.</p>
<% end %>

<%= link_to "Back", my_friends_path, class: "btn btn-primary", id: "btn-white-txt" %>
